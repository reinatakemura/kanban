<div class="usersPage">
  <div class="titleArea">
    <h1>ユーザー</h1>
  </div>
  <div class="container">
    <table>
      <tbody>
        <% @users.each do |user| %>
          <tr>
            <%# <td> %>
<!--              <%= link_to user_path(user) do %> -->
<!--                <% if user == current_user %>
                  <i class="material-icons">person</i>
                <% else %>
                  <i class="material-icons">touch_app</i>
                <% end %> -->
<!--              <% end %> -->
            <%# </td> %>
            <td class><%= user.name %></td>
            <td> has <%= user.lists.length %> lists.</td>
          </tr>
        <% end %>
      </tbody>
    </table>
    <%= paginate(@users) %>
  </div>
</div>
